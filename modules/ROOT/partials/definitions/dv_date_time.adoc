[cols="^1,3,5"]
|===
h|*Class*
2+^h|*DV_DATE_TIME*

h|*Description*
2+a|Represents an absolute point in time, specified to the second. Semantics defined by ISO 8601. 

Used for recording a precise point in real world time, and for approximate time stamps, e.g. the origin of a HISTORY in an OBSERVATION which is only partially known.

h|*Inherit*
2+|`xref:{class_dv_temporal}[DV_TEMPORAL]`, `link:/classes/Iso8601_date_time[Iso8601_date_time] `

h|*Attributes*
^h|*Signature*
^h|*Meaning*

h|*1..1*
|*value*: `link:/classes/String[String] `
a|ISO8601 date/time string.

h|*Functions*
^h|*Signature*
^h|*Meaning*

h|*1..1 +
(effected)*
|*magnitude* (): `link:/classes/Real[Real] `
a|Numeric value of the date/time as seconds since the calendar origin point.

h|*1..1 +
(effected)*
|*is_strictly_comparable_to* ( +
other: `xref:{class_dv_ordered}[DV_ORDERED][1]` +
): `link:/classes/Boolean[Boolean] `
a|Test if two instances are strictly comparable.

h|*1..1 +
(effected)*
|*add* ( +
a_diff: `xref:{class_dv_amount}[DV_AMOUNT][1]` +
): `xref:{class_dv_absolute_quantity}[DV_ABSOLUTE_QUANTITY]`
a|Add a time period to this date_time.

h|*1..1 +
(effected)*
|*subtract* ( +
a_diif: `xref:{class_dv_amount}[DV_AMOUNT][1]` +
): `xref:{class_dv_absolute_quantity}[DV_ABSOLUTE_QUANTITY]`
a|Subtract a time period from this date_time.

h|*1..1 +
(redefined)*
|*diff* ( +
other: `xref:{class_dv_absolute_quantity}[DV_ABSOLUTE_QUANTITY][1]` +
): `xref:{class_dv_amount}[DV_AMOUNT]`
a|Difference of two quantities.

h|*1..1*
|*operator=* ( +
source: `xref:{class_dv_date_time}[DV_DATE_TIME][1]` +
): `xref:{class_dv_date_time}[DV_DATE_TIME]`
a|

h|*1..1*
|*as_string* (): `link:/classes/String[String] `
a|

h|*Invariants*
2+a|__Value_valid__: `valid_iso8601_date_time (value)`
|===
