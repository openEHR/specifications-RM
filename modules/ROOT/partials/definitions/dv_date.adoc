[cols="^1,3,5"]
|===
h|*Class*
2+^h|*DV_DATE*

h|*Description*
2+a|Represents an absolute point in time, as measured on the Gregorian calendar, and specified only to the day. Semantics defined by ISO 8601. Used for recording dates in real world time. The partial form is used for approximate birth dates, dates of death, etc.

h|*Inherit*
2+|`xref:{class_dv_temporal}[DV_TEMPORAL]`, `xref:{class_iso8601_date}[Iso8601_date]`

h|*Attributes*
^h|*Signature*
^h|*Meaning*

h|*1..1 +
(redefined)*
|*value*: `xref:{class_string}[String]`
a|ISO8601 date string.

h|*Functions*
^h|*Signature*
^h|*Meaning*

h|*1..1 +
(effected)*
|*magnitude* (): `xref:{class_integer}[Integer]`
a|Numeric value of the date as days since the calendar origin date `0001-01-01`.

h|*1..1 +
(effected)*
|*is_strictly_comparable_to* ( +
other: `xref:{class_dv_ordered}[DV_ORDERED][1]` +
): `xref:{class_boolean}[Boolean]`
a|Test if two instances are strictly comparable.

h|*1..1 +
(effected)*
|*infix <* ( +
other_quantity: `xref:{class_dv_ordered}[DV_ORDERED][1]` +
): `xref:{class_boolean}[Boolean]`
a|Tests if this date is earlier than the cited date.

h|*1..1 +
(effected)*
|*add* ( +
a_diff: `xref:{class_dv_amount}[DV_AMOUNT][1]` +
): `xref:{class_dv_absolute_quantity}[DV_ABSOLUTE_QUANTITY]`
a|Add a time period to this date.

h|*1..1 +
(effected)*
|*subtract* ( +
a_diif: `xref:{class_dv_amount}[DV_AMOUNT][1]` +
): `xref:{class_dv_absolute_quantity}[DV_ABSOLUTE_QUANTITY]`
a|Subtract a time period from this date.

h|*1..1 +
(redefined)*
|*diff* ( +
other: `xref:{class_dv_date}[DV_DATE][1]` +
): `xref:{class_dv_duration}[DV_DURATION]`
a|Difference of two quantities.

h|*Invariants*
2+a|__Value_valid__: `valid_iso8601_date(value)`
|===
